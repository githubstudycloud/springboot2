app:
  name: Spring Test Application
  description: Application configured via GitLab Config Server
  database:
    url: jdbc:mysql://localhost:3306/testdb
    username: dbuser
    password: '{cipher}encrypted-password-here'  # 如果启用了加密功能
    max-pool-size: 20
